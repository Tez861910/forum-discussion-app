import palette from '../palette';

const sliderStyles = {
  root: {
    color: palette.palette.primary.main,
  },
  thumb: {
    color: palette.palette.primary.main,
  },
  track: {
    color: palette.palette.primary.main,
  },
  rail: {
    color: palette.palette.primary.main,
  },
  mark: {
    color: palette.palette.primary.main,
  },
  markLabel: {
    color: palette.palette.primary.main,
  },
  valueLabel: {
    color: palette.palette.primary.main,
  },
};

export default sliderStyles;
