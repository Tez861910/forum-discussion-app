import palette from '../palette';

const textareaAutosizeStyles = {
  root: {
    margin: '8px',
    padding: '8px',
    border: `1px solid ${palette.palette.default.grey[400]}`,
    borderRadius: '4px',
  },
};

export default textareaAutosizeStyles;
